{% extends 'hote_layout.html.twig' %}

{% block title %}Equipement{% endblock %}

{% block body %}
    <h1>Equipement</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ equipement.id }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ equipement.nom }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('equipement_index') }}">retour</a>

    <a href="{{ path('equipement_edit', {'id': logement.id}) }}">Modifier</a>


{% endblock %}